# v0.4：Linux 与计算机系统的分篇重构

用户指出四篇加长教程仍缺少教材层级，希望在 Linux 入口内涵盖进程线程、流水线、内核、编译原理和 OS。

本轮改为七篇、十四章：
1. 基础接口与工具：进程入口、文件、Shell（三章，保留正文和 URL）。
2. 操作系统与内核：OS 抽象与保护；内核执行路径（两章新正文）。
3. 线程、并发与调度：同步与死锁；调度与资源分配（两章新正文）。
4. 内存、存储与 I/O：虚拟内存；I/O 路径（两章新正文）。
5. 流水线结构与设计：CPU 指令流水线；软件有界数据流（两章新正文）。
6. 编译与程序构建：词法到 IR；目标文件到装载（两章新正文）。
7. 系统观测与工程验收：保留现有完整章，调整到卷末。

目录元数据增加 parts；现有 chapters 仍作为唯一章节列表，parts 仅按 ID 组织，不复制正文。校验篇必须非空、ID 唯一、每章恰好归属一篇、篇顺序与阅读顺序一致。Wiki 首页、卷目录、侧栏、面包屑与打印目录体现篇/章；章内目录显示节/小节缩进。

新增内容是实际 Markdown 正文与图解，不是只有目录。既有“计算机系统”卷保留作为独立的交叉阅读入口，Linux 入口不以跳转该卷代替内容。

无 API、数据库或权限变更；无 Notion 写入、邮件、部署、Git commit/push。原有 URL 保留；不声称覆盖完整 OS/编译器课程或完成真实 Linux 内核/硬件验证。
